# EECS106_Robotics
## TIPs using git
- To change branch : git checkout [branch_name]
- To create a new branch : git checkout -b [branch_name]
- git push/pull origin [branch_name]
- 使用 .gitignore 文件忽略编译后的文件，只保留src 和 .make 文件。 
	- 好处是每次上传的文件大小比较小，且不会受电脑平台限制。(每次需要使用的时候再重新编译)